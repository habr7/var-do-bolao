# ====== App ======
NODE_ENV=development
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Numero do bot no WhatsApp (formato com codigo do pais, sem mascara)
# Usado pra montar links wa.me dos CTAs ("Criar bolao no WhatsApp")
NEXT_PUBLIC_BOT_WHATSAPP_NUMBER=5511978277516

# ====== Backend (Fastify do bot) — apenas Fase 2 do PLANO_SITE ======
# Por enquanto o site e estatico (landing + skeletons). Endpoints abaixo
# so passam a ser usados quando a Fase 2 (web-api no bot) for entregue.

BOT_API_URL=http://localhost:3000
BOT_API_SECRET=changeme-hmac-secret-shared-with-bot

# ====== Sessao (iron-session) ======
# Gere com: openssl rand -hex 32
SESSION_SECRET=changeme-32-bytes-hex-random
SESSION_COOKIE_NAME=vdb_session
